const routes = {
    home: "/",
    auth: "/login",
    profile: "/profile/:uid/*",
    admin: "/admin",
    language: "/language",
    chats: "/chats",
    notFound: "*",
};

export default routes;
